language: c++

branches:
  only:
    - master

dist: trusty

services:
  - docker

before_install:
  - docker pull grpc/cxx

script:
  - docker run -v ${PWD}:/gvs /bin/bash -c "cd /gvs && ./ci/build_and_run.sh"
#  - if [[ "${TRAVIS_OS_NAME}" == "linux" ]]; then make AirWaves_coverage -j; fi;
